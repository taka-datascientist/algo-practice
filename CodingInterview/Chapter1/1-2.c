#include <stdio.h>
#include <string.h>

int sortJudgement(char *str1, char *str2, int length) {
	char compstr1[length], compstr2[length];
	strncpy(compst1, str1, length);
	strncpy(compst2, str2, length);

	
}

int int main() {
	
	return 0;
}